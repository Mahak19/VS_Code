/**BootstrapGridsandButtons.html to call function we'll use oneclick="myfunction() inside." */
// function myfunction(){
    /** alert("Hello");// When we click on button a pop-up will be shown 
    console.log("Hello!");/* When we click on the button buy now then through inspect in console the 
    output will shown,for debuging purposes,not for displaying on page*/
    // document.write("Hello");//Not an ideal way because it opens up a new page and closes the page which is designed,not necessarily the best idea
    // var /*declarative for variable*/ number1/*variable name*/ =1/*IntegerValue*/;
    // let number2=2; //Differnce b/w var and let is of scopes but more corcerned is let over var,both allows to change the value after assigning values to them

    // alert(number1 % number2); //When diving any number no. the output will be in decimal
// }

/**THere are 3 types to define variable var,let,const */